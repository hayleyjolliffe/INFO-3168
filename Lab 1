<!-- STEPS 1 + 2 -->
<html>
	<head>
		<title>Lab 1 — Hayley Jolliffe</title>
		<script>

			// STEP 3: Creating VIDEOTYPE CONSTRUCTOR
			var VideoType = function(title,category,cast,price)
			{
				this.title = title;						// String, "Title"
				this.category = category;				// String, "Genre"
				this.cast = cast;						// String Array, "Cast"
				this.price = price;						// Number, "Price"
				// STEP 4: Initializing numbers to 0
				this.numRents = 0;						// Number, "Rents"
				this.vidRevenue = 0;					// Number, "Revenue"
			}

			// STEP 5: Creating PROTOTYPE
			VideoType.prototype.cancellable = false;
			VideoType.prototype.totRevenue = function()
			{
				// STEP 6: Creating PROCRENTAL
				// Increments rentals, calculates single video revenue, and total revenue
				var procRental = function()
				{

				}
			}

			// STEP 7: Creating ONLOAD FUNCTION
			function StartMeUp()
			{
				// STEP 7 - 2: Creating VIDEOS ARRAY
				var videos = [];

				// STEPS 8 + 9: Injecting sample data into ARRAY
				// Rogue One
				videos.push('Rogue One', 'Sci-Fi', ['Felicity Jones', 'Mads Mikkelson', 'Riz Ahmed'], 17.95);

				// Arrival
				videos.push('Arrival', 'Sci-Fi', ['Amy Adams', 'Jeremy Renner'], 17.95);

				// No Time to Die
				videos.push('No Time to Die', 'Adventure', ['Daniel Craig', 'Lea Seydoux', 'Rami Malek', 'Lashana Lynch'], 28.95);
			}
		</script>
	</head>

	<body onload="StartMeUp()">

	</body>
</html>
